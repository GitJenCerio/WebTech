# MongoDB Connection String
# Get this from your MongoDB Atlas dashboard or your MongoDB server
MONGODB_URI=mongodb://localhost:27017/your-database-name
# For MongoDB Atlas, use format:
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/database-name

# Firebase Configuration (METHOD A - Recommended)
# Get these from Firebase Console -> Project Settings -> Service Accounts
# This method is more secure than storing a JSON file
FIREBASE_PROJECT_ID=your-project-id
FIREBASE_CLIENT_EMAIL=firebase-adminsdk-xxxxx@your-project.iam.gserviceaccount.com
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nYour\nPrivate\nKey\nHere\n-----END PRIVATE KEY-----\n"

# Alternative: Use serviceAccountKey.json file instead (METHOD B)
# If you use the JSON file method, you can leave the above Firebase variables empty

# Migration Settings
# Set to 'true' to preview what will be migrated without writing to database
DRY_RUN=false

# Number of documents to process in each batch (default: 100)
# Lower this value if you experience memory issues
BATCH_SIZE=100
